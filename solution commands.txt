sudo apt-get update
sudo apt-get install ca-certificates curl gnupg -y

sudo install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | \
  sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
sudo chmod a+r /etc/apt/keyrings/docker.gpg

echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] \
https://download.docker.com/linux/ubuntu \
$(. /etc/os-release && echo "$VERSION_CODENAME") stable" | \
sudo tee /etc/apt/sources.list.d/docker.list > /dev/null





sudo apt-get install docker-ce docker-ce-cli containerd.io docker-compose-plugin -y


.yml (file)

opennms:
  image: opennms/opennms:31.1
  depends_on:
    - postgres
  environment:
    INSTALL_OPENNMS_JDBC_URL: jdbc:postgresql://postgres:5432/opennms
    INSTALL_OPENNMS_JDBC_DRIVER: org.postgresql.Driver
    INSTALL_OPENNMS_DB_USER: opennms
    INSTALL_OPENNMS_DB_PASS: opennms123
  volumes:
    - opennms-data:/opt/opennms/etc
    - opennms-logs:/opt/opennms/logs
  ports:
    - "8980:8980"   # Web UI
    - "1162:1162"   # Traps


.yml (file)

opennms:
  image: opennms/opennms:31-stable  # Public, no login needed
  depends_on:
    - postgres
  environment:
    INSTALL_OPENNMS_JDBC_URL: jdbc:postgresql://postgres:5432/opennms
    INSTALL_OPENNMS_JDBC_DRIVER: org.postgresql.Driver
    INSTALL_OPENNMS_DB_USER: opennms
    INSTALL_OPENNMS_DB_PASS: opennms123
  volumes:
    - opennms-data:/opt/opennms/etc
    - opennms-logs:/opt/opennms/logs
  ports:
    - "8980:8980"
    - "1162:1162"
